rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/orbs/{orbId} {
      allow read, write: if request.auth != null 
        && request.auth.uid == userId;
    }
  }
}
